# Older Versions

The first version here is Claude's initial response to the first prompt. Very wonky.

All these archived versions are self-contained HTML pages, no external file dependencies. 

To run them: Download one, then:
```
mkdir ground-assault
mv ground-assault-v6 ground-assault/
cd ground-assault

npx http-server
OR 
python3 ...
```
